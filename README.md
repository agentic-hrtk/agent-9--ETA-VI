This is the README for the agent-9--ETA-VI project.

## Setup

To install the project, run the following command:

```bash
pip install -r requirements.txt
```

## Usage

To use the agent, run the following command:

```bash
python main.py
```
